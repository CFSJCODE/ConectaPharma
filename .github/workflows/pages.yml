name: GitHub Pages disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Pages is not used for production
        run: echo "ConectaPharma uses Firebase Hosting. This workflow is intentionally manual-only."
